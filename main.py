# Boot + main loop

def init():
    pass  # Initialize context, state machine, etc.

running = True

while running:
    # update_context()
    # state = state_machine.current()
    # state.tick(context)
    # watchdog.check()
    pass
